Frontend (React + Tailwind) for AI Meeting Intelligence Platform

Prereqs
- Node 18+

Setup
- npm install
- npm run dev

Config
- Set `VITE_API_BASE` in `.env` to point at FastAPI (default: http://localhost:8000)

